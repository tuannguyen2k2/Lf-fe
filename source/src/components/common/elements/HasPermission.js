import React from 'react';

function HasPermission({ children, permissions, fallback = null }) {
    // const { user } = useAuth();
    // const { permissions: userPermissions } = user || {};

    return <div></div>;
}

export default HasPermission;
